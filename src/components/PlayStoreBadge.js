import styled from "styled-components"

import playBadge from "../images/google-play-badge.png"

const PlayStoreBadge = styled.div`
  width: 240px;
  height: 80px;
  background: url(${playBadge}) no-repeat center;
  background-size: contain;
`

export default PlayStoreBadge
